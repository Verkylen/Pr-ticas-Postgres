SELECT testimonials.id, w.name AS writer, r.name AS recipient, testimonials.message FROM testimonials JOIN users w ON testimonials."writerId" = w.id JOIN users r ON 
testimonials."recipientId" = r.id ORDER BY testimonials.id;